{
  "engines": { "node": ">=20" },
  "private": true,
  "scripts": {
    "build:next": "pnpm --filter ./apps/hookahplus-next... build-export",
    "build:site": "pnpm --filter ./apps/hookahplus-site... build-export",
    "build:all": "pnpm -w run build:next && pnpm -w run build:site"
  }
}
