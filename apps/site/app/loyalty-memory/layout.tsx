export const metadata = {
  title: 'Loyalty & Memory | Hookah+ - Remember People, Not Just Points',
  description: 'Loyalty is remembering people, not just points. Hookah+ remembers flavor mixes, visit patterns, and preferences — so every guest feels known, even when staff changes.',
  keywords: 'hookah lounge loyalty, guest memory, customer retention, behavior-based loyalty, flavor tracking, visit patterns, VIP management',
};

export default function LoyaltyMemoryLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
