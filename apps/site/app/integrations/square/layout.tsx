export const metadata = {
  title: 'Square Integration | Hookah+ - Memory Above Your POS',
  description: 'Square handles payments. Hookah+ handles memory. Great hospitality is built on memory, not transactions. Hookah+ adds session tracking, guest memory, and loyalty above Square.',
  keywords: 'Square integration, hookah lounge Square, Square POS integration, session management Square, guest memory Square',
};

export default function SquareIntegrationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
