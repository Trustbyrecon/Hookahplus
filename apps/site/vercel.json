{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm build --filter=@hookahplus/site",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/site/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
