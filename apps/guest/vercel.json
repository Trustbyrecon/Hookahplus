{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm build --filter=@hookahplus/guest",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/guest/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
