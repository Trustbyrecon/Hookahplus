{
  "version": 2,
  "buildCommand": "pnpm --filter @hookahplus/app run build",
  "outputDirectory": "apps/app/.next",
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "env": {
    "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD": "1"
  }
}