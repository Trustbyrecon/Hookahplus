{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && cd apps/app && pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/app/.next"
}
