{
  "version": 2,
  "buildCommand": "cd apps/site && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "apps/site/.next",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
