{
  "version": 2,
  "buildCommand": "cd apps/site && pnpm install --no-frozen-lockfile && pnpm build",
  "outputDirectory": "apps/site/.next",
  "installCommand": "cd apps/site && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
