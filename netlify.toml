# Netlify configuration for HookahPlus deployment
[build]
base = "hookahplus-v2-"
command = "npm ci --legacy-peer-deps && npm run build"
publish = "out"

[build.environment]
NODE_VERSION = "18"
NPM_FLAGS = "--legacy-peer-deps"
