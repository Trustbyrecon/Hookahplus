# Netlify configuration for HookahPlus deployment with static export
[build]
command = "npm ci --legacy-peer-deps && npm run build"
publish = "out"

[build.environment]
NODE_VERSION = "20"
NPM_FLAGS = "--legacy-peer-deps"
